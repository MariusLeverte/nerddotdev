import { Container } from "@nextui-org/react";
import { motion, useCycle } from "framer-motion";

const Start = () => {
  return (
    <Container
      css={{ height: "100vh" }}
      display="flex"
      alignItems="center"
    ></Container>
  );
};

export default Start;
